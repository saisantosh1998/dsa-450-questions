class Reverse
{
    // Complete the function
    // str: input string
    public static String reverseWord(String str)
    {
        int n=str.length();
        String res="";
        for (int i=n-1;i>=0;i--){
            res+=str.charAt(i);
        }
        return res;
    }
}
